{
  "name": "quell-adblock",
  "version": "0.1.0",
  "private": true,
  "description": "Quell — a Manifest V3 ad blocker for Chromium browsers aiming for uBlock Origin blocking parity within the MV3 rules.",
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "compile-filters": "node scripts/compile-filters.mjs",
    "update-lists": "node scripts/update-lists.mjs",
    "bundle": "node scripts/build.mjs",
    "build": "npm run compile-filters && npm run bundle",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "dev": "npm run build && npm run watch"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  }
}
